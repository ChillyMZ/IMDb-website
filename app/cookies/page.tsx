import InfoShell from '../info-shell';
import CookiePreferences from '../cookie-preferences';
export const metadata={title:'Cookie preferences | ChillyMZ',description:'Choose whether ChillyMZ may collect optional aggregate usage statistics.'};
export default function Cookies(){return <InfoShell title="Cookies & preferences"><p>Sign-in and security cookies are provided by the hosting and authentication service. They are needed to access this private site and are not controlled by the analytics buttons below.</p><p>ChillyMZ stores your optional analytics choice in a cookie called <code>chillymz_metrics</code>. It contains only “allow” or “deny” and lasts up to six months. The optional analytics system does not create a tracking identifier or send data to an advertising network.</p><CookiePreferences page/><h2>Other browser storage</h2><p>The social demo may keep sample activity and interface preferences in your browser. Demo activity is separate from the live catalogue and community scores. Browser settings can clear locally stored preferences; clearing sign-in cookies may sign you out.</p><p>See the <a href="/privacy">privacy policy</a> for account information, hosting, retention and contact details.</p></InfoShell>}
