import {SITE_URL,SITE_INDEXABLE} from './site-config';
export function httpsRedirect(request:Request){const url=new URL(request.url);if(url.hostname===new URL(SITE_URL).hostname&&url.protocol==='http:'){url.protocol='https:';return Response.redirect(url.toString(),308)}return null}
export function secureResponse(request:Request,response:Response){const headers=new Headers(response.headers);headers.set('X-Content-Type-Options','nosniff');headers.set('Referrer-Policy','strict-origin-when-cross-origin');headers.set('Permissions-Policy','camera=(), microphone=(), geolocation=()');headers.set('Content-Security-Policy',"base-uri 'self'; object-src 'none'");if(!SITE_INDEXABLE)headers.set('X-Robots-Tag','noindex, nofollow');if(new URL(request.url).hostname===new URL(SITE_URL).hostname)headers.set('Strict-Transport-Security','max-age=31536000');return new Response(response.body,{status:response.status,statusText:response.statusText,headers})}
