import {SITE_URL,SITE_INDEXABLE} from '../site-config';
export function GET(){const paths=SITE_INDEXABLE?['/','/privacy','/terms','/cookies']:[];return new Response('<?xml version="1.0" encoding="UTF-8"?><urlset xmlns="http://www.sitemaps.org/schemas/sitemap/0.9">'+paths.map(p=>`<url><loc>${SITE_URL}${p}</loc></url>`).join('')+'</urlset>',{headers:{'Content-Type':'application/xml; charset=utf-8'}})}
