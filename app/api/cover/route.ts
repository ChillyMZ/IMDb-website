import {headers} from 'next/headers';
import {getChatGPTUser} from '../../chatgpt-auth';
import {storage} from '../../core-store';
import {OWNER_EMAIL} from '../../core-service';
export async function GET(request:Request){try{const user=await getChatGPTUser(),h=await headers(),id=h.get('oai-authenticated-user-id');if(!user||!id)return new Response('Sign in required',{status:401});const {db,bucket}=storage();const b=await db.prepare('SELECT owner,status,cover_key,mime FROM books WHERE id=?').bind(new URL(request.url).searchParams.get('id')||'').first();if(!b||(b.status!=='approved'&&b.owner!==id&&user.email.toLowerCase()!==OWNER_EMAIL))return new Response('Not found',{status:404});const obj=await bucket.get(b.cover_key);if(!obj)return new Response('Not found',{status:404});return new Response(obj.body,{headers:{'Content-Type':b.mime,'X-Content-Type-Options':'nosniff','Cache-Control':'private, no-store'}})}catch(e){console.error('Cover load failed',e);return new Response('Cover unavailable',{status:503})}}
